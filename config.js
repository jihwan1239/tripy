/* =========================================================
   삿포로 여행 일정 — 관리자 설정 파일
   ---------------------------------------------------------
   이 파일의 값만 채우면 관련 기능이 켜집니다.
   비워두면 해당 기능은 "API 비활성화 상태"가 되고,
   지도는 키가 필요 없는 기본 지도(OpenStreetMap)로 표시됩니다.

   ⚠️ 이 파일은 브라우저에 그대로 노출됩니다.
      공개 저장소에 올릴 키는 반드시 사용 제한을 걸어주세요.
      자세한 내용은 README.md의 "보안" 항목을 확인하세요.
   ========================================================= */

window.APP_CONFIG = {

  /* ========== 1. 지도 ========== */

  /* Google Maps JavaScript API 키.
     비워두면 기본 지도(OpenStreetMap)가 표시됩니다. */
  GOOGLE_MAPS_API_KEY: "",

  /* true로 두면 위 키를 이용해 실제 경로 기반 소요시간을 계산합니다.
     (Google Cloud에서 Directions API도 함께 활성화 필요)
     false거나 지도 키가 없으면 거리 기반 추정치로 표시됩니다. */
  ENABLE_DIRECTIONS: false,


  /* ========== 2. AI 장소 추천 ========== */

  /* 프록시 서버 주소.
     OpenAI 키를 이 파일에 직접 넣으면 누구나 가져다 쓸 수 있으므로,
     키를 보관하는 작은 서버를 하나 두고 그 주소만 여기 적습니다.
     서버 코드는 ai-proxy/ 폴더에 있고, 설정 방법은 README를 보세요.

     예: "https://sapporo-ai.내계정.workers.dev"
     비워두면 각자 자기 키를 입력해서 쓰는 방식으로 동작합니다. */
  AI_PROXY_URL: "",

  /* 프록시가 없을 때, 사용자가 직접 자기 OpenAI 키를 넣어 쓸 수 있게 할지 여부.
     키는 그 사람 기기에만 저장되고 다른 사람에게 전달되지 않습니다.
     false로 두면 AI 추천 기능이 완전히 비활성화됩니다. */
  ALLOW_PERSONAL_AI_KEY: true,

  /* 개인 키 방식일 때 사용할 모델명 */
  OPENAI_MODEL: "gpt-4o-mini",


  /* ========== 3. 친구들과 공유 (동기화) ==========

     아래 3개를 채우면 일정이 클라우드에 저장되어
     친구들과 같은 일정을 함께 보고 수정할 수 있습니다.
     비워두면 "내 기기에만 저장" 모드로 동작합니다.
     (그래도 공유 링크로 일정을 전달하는 건 가능합니다)

     설정 방법은 README.md의 "친구들과 공유하기" 항목을 보세요. */

  SUPABASE_URL: "",        // 예: https://xxxxxxxx.supabase.co
  SUPABASE_ANON_KEY: "",   // Supabase의 anon public 키
  TRIP_ID: "sapporo-2026"  // 여행방 이름. 친구들과 같은 값을 쓰면 같은 일정을 봅니다.

};
